# Передача лендинга 3D-стикеров в разработку

Передан 11.08.2026, обновлён 24.08.2026. Что изменилось после передачи — раздел 0.

Прототип: `stickers-landing/stickers-v2.html` — одностраничник. Разметка в нём, стили
и скрипты вынесены в `assets/stickers.css` и `assets/stickers.js`, токены и шрифты общие
с каталогом (`../assets/css/tokens.css`, `fonts.css`). При правке css/js бампать `?v=`
в ссылках, иначе браузер отдаст старый файл. Верстался как эталон: то, что видно в браузере, и есть
согласованный макет. Дизайн-система общая с каталогом — `assets/css/tokens.css`
и `assets/css/fonts.css`.

Сопутствующие документы в этой же папке:
- `AUDIT.md` — технический аудит с замерами, что исправлено и что осталось;
- `FUNNEL.md` — воронка, сегменты, роли двух форм, правила калькулятора;
- `ANSWERS.md` — дословные ответы заказчика: цены, сроки, условия доставки.

---

## 0. Что изменилось после передачи (24.08)

Документ передавался 11.08. После этого пришёл пакет правок маркетолога и был сделан
второй технический аудит. Если вёрстка уже разбирается на шаблон — перечитать этот раздел
и заново снять `stickers-v2.html`, `assets/stickers.css`, `assets/stickers.js` и картинки
`models/card-m*.webp`.

**Порядок блоков другой.** Было: герой → «для брендов» (логотипы внутри) → виды форм →
… → отдельный блок «Смолу льют две линии». Стало: герой → «Создаём мерч вместе с брендами»
(`#o-nas`) → «С кем мы работали» (`#clients`) → «3D-стикеры нужны каждому бренду» (`#b2b`) →
дальше как было. Отдельного блока про линии больше нет: его два фото и восемь карточек-фактов
переехали в `#o-nas`. Логотипы клиентов стали самостоятельной секцией `#clients` с собственным
h2. Актуальная таблица — в разделе 1.

**Появилась новая секция `#clients`** — под неё нужно поле в админке (ряд логотипов
+ плашка «Место для вашего стикера»).

**Пунктов меню шесть, порядок пунктов обязан совпадать с порядком секций.** Сейчас:
Бизнесу, Виды, Расчёт, Портфолио, Контакты, Вопросы. У `#o-nas`, `#clients`, преимуществ
и отзывов своего пункта нет — подсветка держится на ближайшем предыдущем, так задумано.

**Тексты почти везде новые** — первый экран, карточки «Бизнесу», заголовок и карточки
преимуществ, текст «О нас». Лимиты в разделе 2 пересняты под них, часть выросла.

**Новые элементы, которых не было в переданной версии:**
- надзаголовок «Контакты» в блоке шоурума, там же строкой телефон и почта
  (цели `phone` / `mail`, `place=showroom`);
- плашка «У вас большой тираж?» под калькулятором с кнопкой «Обсудить проект»;
- ссылка «Условия доставки, самовывоз и оплата» под калькулятором — теперь одна
  и с весом (`.calc__link--lead`), из калькулятора убрана вторая ссылка на оферту;
- на ширинах до 560 px восемь карточек в `#o-nas` идут горизонтальной лентой,
  а не столбиком (только медиа-запрос, разметка та же).

**Технические правки аудита, которые видно в коде:**
- иконка в шаге калькулятора вставляется только там, где она есть. Раньше `<img>`
  создавался для всех шагов и давал 12 запросов `calc-icons/size-*.webp`, `qty-*.webp`
  в 404 на каждой загрузке;
- акция сравнивается с текущей датой, а не с захардкоженным 30 июля. Раньше условие было
  истинным всегда, то есть скидка не выключилась бы никогда. **Акция кончается 31.08** —
  после этой даты расчёт и обе плашки гаснут сами;
- подписи под цифрами на первом экране переведены на `--v-text-warm`: было 3,54 при норме
  4,5, стало 7,08;
- допуск подсветки активного пункта меню `probe + 4` → `probe + 16`: с прежним активным
  оставался предыдущий пункт;
- у кнопки шапки вернулся собственный `transition` — общее правило объёмных кнопок стоит
  в файле позже и затирало его.

---

## 1. Состав страницы

Порядок блоков сверху вниз. «Правится» — то, что редактор меняет в админке
без разработчика.

| Блок | Якорь | Из чего состоит | Правится |
|---|---|---|---|
| Герой | — | Надстрочник, заголовок, лид, две кнопки, 3D-модель, три факта с подписями, плашка акции | Тексты, факты, акция |
| О нас | `#o-nas` | Заголовок, текст, 2 фото, 8 карточек-фактов (до 560 px — лента) | Текст, фото, карточки |
| С кем мы работали | `#clients` | 11 логотипов + плашка «Место для вашего стикера» | Логотипы |
| Бизнесу | `#b2b` | Заголовок, лид, фото продукта, 4 карточки с бейджами, фото-сравнение из 3 карточек | Карточки, лид, фото |
| Виды и отделка | `#vidy` | Сетка 3×2 из 6 карточек (4 формы + 2 отделки), на ≤860 px — горизонтальная лента со снапом; у «Круглой формы» жёлтая обводка и бейдж «Хит»; ссылка «Посчитать тираж» | Карточки и картинки |
| Калькулятор | `#calc` | Плёнка, размер, тираж, допы, сводка, форма, плашка «У вас большой тираж?», ссылка на условия | **Прайс — только разработчик**, тексты подписей и плашки |
| Портфолио | `#portfolio` | 8 работ, клик открывает лайтбокс | Фото |
| Преимущества | `#preimushchestva` | 6 карточек + 4 медиа-ячейки (2 фото, 2 видео) | Карточки, медиа |
| Отзывы | `#reviews` | Рейтинг, 4 карточки, 6 слотов под скриншоты | Всё |
| Шоурум | `#showroom` | Надзаголовок «Контакты», адрес, часы, телефон и почта, ссылка на карты | Всё |
| ЧАВО | `#faq` | 7 вопросов на `<details>`, форма заявки | Вопросы и ответы |
| Подвал | — | Контакты, разделы, документы | Контакты и ссылки |

Порядок в таблице = порядок на странице. Менять его без разговора с дизайном нельзя:
он собран под воронку (`FUNNEL.md`) и в нынешнем виде согласован маркетологом 24.08.

Плюс две модалки — «Оформить заказ» и «Доставка и самовывоз» — и плашка cookie.

---

## 2. Лимиты текста

Замерено по вёрстке. Первая цифра — сколько знаков в макете сейчас,
вторая — сколько можно писать, не ломая ритм блока.

| Поле | Сейчас | Держаться в |
|---|---|---|
| Карточка «Бизнесу»: бейдж | 16–20 | до 24, одной строкой |
| Карточка «Бизнесу»: заголовок | 19–43 | **до 50** — на 51 знаке уходит в третью строку |
| Карточка «Бизнесу»: текст | 99–141 | до 150 |
| Лид блока «Бизнесу» | 102 | до 120 — дальше третья строка и шапка блока разъезжается |
| Сравнение: заголовок | 16–19 | до 24 |
| Сравнение: подпись | 55–70 | до 80 |
| Вид стикера: заголовок | 6–19 | до 24, одно слово не длиннее 16 |
| Вид стикера: текст | 47–87 | до 90, под текстом зарезервированы 3 строки |
| Преимущество: бейдж | 6–22 | до 24, одной строкой |
| Преимущество: заголовок | 19–51 | до 55 (три строки — норма для этого блока) |
| Преимущество: текст | 27–66 | до 70; у трёх карточек текста нет вовсе, это допустимо |
| Факт о производстве: заголовок | 16–26 | до 28 — дальше вторая строка |
| Факт о производстве: текст | 50–153 | до 155 |
| Вопрос ЧАВО | 23–42 | до 45 |

Ничего не обрезается многоточием: при переполнении блок растёт, а карточки в ряду
выравниваются по самой высокой. Поэтому лимиты — не защита от поломки, а защита
от разнобоя.

Цифры пересняты 24.08 рендером на 1440 px после правок маркетолога: «сейчас» — это
фактическая длина в макете, «держаться в» — граница, за которой добавляется строка.
Текст «О нас» — единственное длинное поле: 534 знака, ограничение не по строкам,
а по здравому смыслу — это второй блок страницы, и он и так самый длинный.

---

## 3. Что делает разработчик

**Формы — три штуки, отправки нет ни у одной.** Это сделано намеренно и написано
прямо в интерфейсе: «это прототип, заявка никуда не уходит». Нужна интеграция
с Битриксом. Валидация, состояния ошибок, блокировка повторной отправки и экран
успеха уже написаны — трогать не нужно, только подключить отправку.

**«Заказать образец» в первом экране ведёт не на страницу, а в шторку заказа.** Ссылка
на кнопке есть (`#zayavka`) и работает как запасной путь без JS, но обработчик её
перехватывает и открывает ту же модалку «Оформить заказ» в режиме образца: заголовок
меняется на «Заказать образец», вместо сводки расчёта одна строка «Пробный стикер, 1 шт»,
допы из калькулятора не подставляются, а в скрытое поле `source` уходит `sample`.
Так сделано по воронке (`FUNNEL.md`, раздел «Образец»): образец — реальная услуга,
печатаем 1 шт, а калькулятор меньше 10 шт не считает, поэтому провести образец через
расчёт нельзя, и отдельную четвёртую форму под него не заводили.

**Ветка лида видна по скрытому полю `source`:** `zayavka` — блок «Нестандартная задача»
и ЧАВО, `calc` — шторка после расчёта, `sample` — та же шторка, открытая кнопкой образца.
Поле должно доезжать до CRM, иначе все заявки склеятся в одну ветку.

**Калькулятор считает по таблице в коде.** Прайс лежит в `assets/stickers.js`, объект
`PRICE`: три плёнки, пять размеров, семь ступеней тиража, плюс отдельная таблица
стикерпаков. Правится только там. Контрольные значения для сверки после переноса —
в разделе 7 ниже.

**Две внешние зависимости в критическом пути.** `model-viewer` с unpkg (283 КБ плюс wasm
и воркеры) и HDR-окружение с `modelviewer.dev` (447 КБ). Если любой из хостов недоступен,
первый экран остаётся без стикеров. Для прода оба файла положить локально: пакет через npm,
HDR — файлом в проект.

**Счётчик Метрики не стоит, но цели размечены.** Все точки конверсии помечены `data-goal`,
вызовы идут через одну функцию в начале `assets/stickers.js`. Разработчику остаётся вставить сниппет,
вписать номер в `YM_ID` и завести цели по именам — см. [ANALYTICS.md](ANALYTICS.md).
Решение «использовать 91973 или заводить новый» за заказчиком.

**Акция считается по текущей дате и заканчивается 31.08.** Дата лежит в `BIZ.promo`
(`assets/stickers.js`), скидка −30% накидывается поверх прайса при тираже от 1 000 штук
(от 500 паков). После истечения гаснут и расчёт, и обе плашки — на первом экране
и в калькуляторе. Что с акцией после 31 августа, решает заказчик; если её продлевают,
меняется только дата.

**Перед выкладкой снять `noindex`** в мете и заменить `https://ДОМЕН/` в структурированных
данных на реальный адрес — там два места в JSON-LD. Плюс добавить `canonical`, `og:url`
и сделать `og:image` абсолютным.

---

## 4. Чего ждём от заказчика

1. **Домен** — без него не закрыть SEO-разметку и шаринг.
2. **Решение по счётчику** Метрики.
3. **Судьба акции после 31.08** — продлевать, менять условия или снимать.
4. Фото для блока «Сравните сами» — ждём их кадры. Нужно ещё решить, все три композиции
   снимаем на одном предмете или на разных: если носители разные, человек сравнивает
   предметы, а не стикеры на них.
5. Скриншоты реальных отзывов под слоты в блоке отзывов.

Раздел «Дополнительно» в калькуляторе из этого списка убран — сделали сами 25.08, ждать
ответа было нечего. Стало два шага: «Как отдадим тираж» (радио: листом / поштучно /
по одному, по умолчанию «листом») и «Дополнительно» с одной галочкой «Нужен дизайн».
В заявку уходят те же флаги, что раньше давали чекбоксы.

Тексты маркетолога пришли 24.08 и уже внесены — этот пункт закрыт. Осталось расхождение,
которое она просила перепроверить у производства: в блоке «О нас» стоит «более 200 000
стикеров в месяц», в карточке рядом — «тираж до 50 000, на пятьдесят тысяч берём месяц».
Оба числа видны в одном экране. Оставлено как написано, вопрос открыт.

---

## 5. Грабли, на которые мы уже наступили

**Медиа подставляется по имени файла.** Блоки с `data-media`, `data-work`, `data-shot`,
`data-logo` сами ищут файл: `adv/v1.webm`, `works/w1.webp` и так далее. Нет файла —
остаётся заглушка, ничего не ломается. При переносе в CMS эту логику надо сохранить
или заменить на обычные поля, но тогда заглушки тоже нужно предусмотреть.

**Видео только в webm.** mp4 не собран, поэтому в Safari ролики не проигрываются —
вместо них показывается постер, первый кадр того же ролика. Если нужен реальный
автоплей во всех браузерах, собрать mp4.

**png лежит в `.gitignore`** по всему репозиторию — исходники кадров в git не попадают,
только webp. Это уже приводило к потере исходников.

**Кэш стилей.** При правке `assets/css/*` бампать `?v=` в ссылках, иначе браузер отдаёт
старый CSS с новой разметкой и вёрстка выглядит сломанной.

---

## 6. Приёмка

- Все три формы отправляют заявку в Битрикс, письмо и лид приходят.
- В лиде виден `source`: `zayavka`, `calc` и `sample` приходят разными значениями.
- «Заказать образец» открывает шторку в режиме образца («Пробный стикер, 1 шт»), а не форму с расчётом.
- Калькулятор считает те же суммы, что прототип: контрольные значения в разделе 7.
- Метрика стоит, цели из ANALYTICS.md срабатывают (проверяются в консоли через `window.__goals`).
- `noindex` снят, `canonical` и `og:url` заполнены, в JSON-LD нет плейсхолдеров.
- Модалки: закрытие по Escape и клику по фону, возврат фокуса, `inert` на фоне.
- Портфолио открывается лайтбоксом, закрывается по Escape.
- На 390 px нет горизонтальной прокрутки, поля ввода 16 px и iOS не зумит форму.
- Контраст текста не ниже 4,5:1.
- Акция гаснет сама после даты окончания: переведите дату назад и убедитесь, что скидки
  нет ни в расчёте, ни на плашке первого экрана.
- В консоли и в сети нет 404: иконки шагов калькулятора запрашиваются только там, где есть.
- На ширинах до 560 px карточки блока «О нас» идут горизонтальной лентой, а не столбиком.
- `model-viewer` и HDR-окружение отдаются со своего домена, а не с unpkg и modelviewer.dev.

---

## 7. Прайс: контрольные значения

Таблица снята с прототипа 21.08 **при выключенной акции** — это чистые цены по прайсу.
После переноса калькулятор должен выдавать ровно эти цифры. Если сошлись эти
шестнадцать комбинаций, сойдётся и остальное: они покрывают все три плёнки, крайние
размеры, крайние тиражи и обе границы, где логика меняется.

| Плёнка | Размер | Тираж | За штуку | За тираж | Срок |
|---|---|---|---|---|---|
| Белый глянец | 10×10 | 100 | 40 ₽ | 4 000 ₽ | 3–7 рабочих дней |
| Белый глянец | 10×10 | 1 000 | 5,60 ₽ | 5 600 ₽ | 7 рабочих дней |
| Белый глянец | 10×10 | 50 000 | 2 ₽ | 100 000 ₽ | около месяца |
| Белый глянец | 20×20 | 1 000 | 10 ₽ | 10 400 ₽ | 7 рабочих дней |
| Белый глянец | 50×50 | 1 000 | 40 ₽ | 39 950 ₽ | 7 рабочих дней |
| Белый глянец | 50×50 | 50 000 | 33 ₽ | 1 661 500 ₽ | около месяца |
| Голография | 10×10 | 100 | 44 ₽ | 4 437 ₽ | 3–7 рабочих дней |
| Голография | 20×20 | 1 000 | 13 ₽ | 13 400 ₽ | 7 рабочих дней |
| Голография | 50×50 | 1 000 | 63 ₽ | 62 540 ₽ | 7 рабочих дней |
| Голография | 50×50 | 50 000 | 57 ₽ | 2 834 500 ₽ | около месяца |
| Фотолюминесцентная | 10×10 | 1 000 | 12 ₽ | 11 800 ₽ | 7 рабочих дней |
| Фотолюминесцентная | 20×20 | 100 | 56 ₽ | 5 600 ₽ | 3–7 рабочих дней |
| Фотолюминесцентная | 50×50 | 1 000 | 60 ₽ | 60 230 ₽ | 7 рабочих дней |
| Фотолюминесцентная | 50×50 | 50 000 | 51 ₽ | 2 525 000 ₽ | около месяца |
| Стикерпак А7 | — | 100 паков | 171 ₽ / пак | 17 100 ₽ | — |

**Минимальный чек.** Белый глянец 20×20: 10, 50 и 100 штук стоят одинаково — 4 020 ₽
(402 ₽, 80 ₽ и 40 ₽ за штуку соответственно). На 500 штуках сумма впервые растёт, 7 150 ₽.
Это не ошибка расчёта, а свойство прайса, и его надо сохранить при переносе.

**Правила, без которых цифры не сойдутся:**

- Интерполируется **сумма за тираж**, а не цена за штуку. Если тянуть прямую между ценами
  за штуку, на промежуточных тиражах цена начнёт расти с ростом тиража.
- Цена за штуку округляется до копеек, пока она меньше 10 ₽, дальше до рубля. Округление
  до рубля на больших тиражах завышало бы на десятую часть и расходилось бы с суммой.
- Свой размер считается по стороне эквивалентного квадрата, границы 10 и 50 мм: столбцы
  прайса — квадраты, поэтому по площади считать нельзя.
- Акция **−30% сверху** от прайса, при тираже от 1 000 штук (от 500 паков), до 31.08.
  Она не заложена в цены, а накидывается после расчёта. Значения в таблице выше — без неё.
- Все цифры на странице подписаны «от», это осознанно и менять нельзя.

**Две особенности прайса, которые выглядят как ошибка, но ею не являются.** Фотолюминесцентная
местами дешевле голографической: голография печатается УФ и почти не дешевеет с тиражом.
И одна спорная клетка в голографии на 50×50 держится по листу от 06.08 — в исправленном файле
от 07.08 она поехала попутно и противоречила остальному прайсу. В коде рядом стоит комментарий.
