# Админка: поля, правила, крайние значения

Договорённость от 17.07: **ни один блок, поле или текст не уходит в разработку без ответа «кто и
как это администрирует»**. Здесь ответ по каждому блоку страницы каталога.

Читать вместе с: `TEMPLATES.md` (какие блоки у какого типа страницы), `DESIGN_SYSTEM.md` §10
(правило двух имён, лимиты, почему они нужны).

---

## 0. Как читать таблицы

| Колонка | Что значит |
|---|---|
| **Поле** | что видит редактор в админке |
| **Тип** | текст / текст.область / медиа / связь / повторитель / флаг |
| **Об.** | обязательное |
| **Лимит** | максимум знаков; **это не каприз** — при превышении ломается сетка (см. «Что если длиннее») |
| **Пусто** | что происходит, если редактор не заполнил |

**Общее правило пустых значений:** пустое необязательное поле → элемент **не рендерится** (не
пустая плашка, не «—»). Пустой блок целиком → секция не выводится и **её пункт исчезает из
подшапки-навигации** (она собирается из включённых блоков, а не из статичного списка).

**Общее правило количества элементов (19.08).** У каждого блока с карточками есть минимум и
максимум — они в разделе блока, в «Крайних значениях». Ниже минимума секция **не выводится**
целиком. Сверх максимума **режет шаблон**, а не админка: лимит в поле — подсказка редактору, но
контент может приехать импортом мимо админки, и тогда единственная защита сетки — вывод.
Числа взяты не на глаз: сетки замерены рендером на 390 / 1024 / 1280 / 1440, разбор с цифрами —
в `PROGRESS.md` за 19.08.

**Как посмотреть крайние состояния.** В прототипе контент один, и граничные случаи не видны:
карусель «Видов» включается с четвёртой карточки, кнопка «Показать ещё» — с седьмой плитки,
растяжка статей — при 2–4. Состояние задаётся адресом:

- `prototypes/usluga.html?limits=vidy5` — «Виды» лентой со стрелками
- `prototypes/usluga.html?limits=prod12` — 12 плиток «Где применяется» (на мобилке появится кнопка)
- `prototypes/usluga.html?limits=art2,combo2` — несколько блоков сразу

Ключи: `vidy`, `price`, `prod`, `combo`, `art`; число — сколько карточек оставить. Карточки при
этом клонируются по кругу и повторяются: это стенд состояний, а не контент. **Демо-скрипт живёт в
начале `<script>` и при переносе в CMS удаляется.**

**Общее правило длинного текста:** лимит в админке — жёсткий (счётчик + запрет сохранения). Мягкая
страховка в вёрстке (обрезка многоточием) есть НЕ везде: `-webkit-line-clamp` не работает на
флекс-элементе (флекс блокифицирует `display`), поэтому местами текст просто вытолкнет соседей.
Лимит — основной механизм, вёрстка — второй эшелон.

---

## 1. Типы записей и таксономии

| Сущность | Тип | Зачем |
|---|---|---|
| Страница каталога | CPT `catalog_page` с полем **Тип**: `технология` (=`услуга`) · `материал` | один гибкий шаблон, два типа (см. `TEMPLATES.md`) |
| Группа товаров | таксономия `product_group` | «Визитки», «Папки» — то, что показывает блок «Где применяется» |
| Раздел каталога | таксономия `catalog_section` | верхний уровень: «Печать полиграфии» |
| Работа портфолио | CPT `work` | видео и фото работ |
| Статья | CPT `post` (существует) | блок «Статьи» |

### Поля таксономии `product_group` (важно!)

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Название (навигационное) | текст | да | — | — |
| **`short_name`** (клиентское) | текст | нет | **20** | фолбэк на название |
| Картинка | медиа | да | — | плитка не рендерится |
| Применимо: тиснение / УФ-лак / … | флаги | нет | — | группа не попадает в «Где применяется» |

**Почему два имени.** Навигационное живёт в меню и URL («Многостраничная полиграфия»),
клиентское — в плитках и тексте («Каталоги и брошюры»). Первое оптимизировано под поиск по
каталогу, второе — под чтение. **Клиентское имя живёт у группы, а не у страницы:** редактор правит
один раз, меняется везде. Если привязать к странице — одна сущность окажется названа по-разному на
пяти страницах, и никто не найдёт правду.

---

## 2. Блоки страницы

### 2.1. Герой

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Надзаголовок (eyebrow) | текст | нет | 30 | не рендерится |
| H1 | текст | да | **27 знаков** | — |
| Лид | текст.область | да | 160 | — |
| Буллеты | повторитель (3–5) | нет | 40 / шт | блок буллетов не рендерится |
| Кадр | медиа | да | — | плейсхолдер |
| CTA 1: подпись | текст | да | 24 | — |
| CTA 2: подпись | текст | нет | 24 | вторая кнопка не рендерится |

**Что если длиннее.** H1 в 27ch — это ширина экрана 390 при мобильной ступени 34px. Длиннее —
заголовок упрётся в край. Крошки собираются автоматически из иерархии; последняя обрезается
многоточием, средние схлопываются в «…» — тут лимит не нужен.

**Крайние значения.** Буллетов 0 → блок скрыт; 6+ → запретить: ломает высоту героя рядом с кадром.

**Вторая кнопка героя выключается пустой подписью.** В эталоне это «Заказать образец»
рядом с «Оставить заявку на расчёт»: первая кнопка основная (жёлтая), вторая — контурная.
Обе открывают модалку заявки, отдельной страницы за ними нет: вторая — ту же форму звонка,
но с заголовком «Заказать образец», кнопкой «Жду образец» и своим значением `context`,
чтобы заявку на образец было видно в CRM отдельно. Тексты этой подмены — тоже поля шаблона. Если подпись пустая, кнопки
просто нет: раскладка не едет, первая остаётся на своём месте и своей ширины (проверено
рендером на 1440 и 390). Ряд кнопок переносится, поэтому две подписи по 24 знака помещаются
и на десктопе; на мобилке кнопки идут в столбик каждая на всю ширину.

**Порядок нельзя менять местами.** Основное действие всегда первое: жёлтая кнопка задаёт,
куда ведёт страница. Вторая — запасной, более мягкий вход, и если она станет основной,
страница начнёт продавать образец вместо тиража.

### 2.2. Статбар

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Пара «цифра + подпись» | повторитель, **ровно 4** | да | цифра 6, подпись 30 | — |

**Крайние значения.** Ровно 4: на мобилке это сетка 2×2, на десктопе ряд из четырёх с
разделителями. 3 или 5 — сетка развалится. Если данных на 4 нет — блок выключается целиком.

### 2.3. Преимущества

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Плитка: иконка | выбор из набора | да | — | — |
| Плитка: заголовок | текст | да | 30 | — |
| Плитка: текст | текст.область | да | 120 | — |
| Кадр блока | медиа | нет | — | колонка с кадром скрыта |

**Крайние значения.** 4 плитки (2×2 на десктопе, 1 колонка на мобилке). 3 → последняя строка
неполная, допустимо. 5+ → запретить.

**Кадр: размер диктует колонка, а не файл (19.08).** Высота медиа-колонки на десктопе равна высоте
столбика плиток (нижняя граница — `min-height:300px`), на мобилке у неё `aspect-ratio:4/3`.
Картинка вписывается с обрезкой по центру: `position:absolute; inset:0; object-fit:cover` —
абсолют обязателен, иначе `height:100%` упирается в высоту `auto` у родителя и колонка снова
растёт по файлу. Без этого правила вертикальный кадр 600×1200 растил секцию с 572 до 1446px и
вылезал за свою колонку — замерено.

**Что грузить редактору.** Горизонтальный кадр, ориентир 4:3, не меньше 1200×900. Вертикальное
фото вёрстку не сломает, но у него срежется верх и низ — если важен весь кадр, он потеряется.
Точку обрезки редактор не выбирает: обрезаем по центру, отдельного поля «фокус кадра» не
заводим (для макро-съёмки отделки центр почти всегда верен).

### 2.4. Виды

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Карточка: заголовок | текст | да | **30** | — |
| Карточка: описание | текст.область | да | **130** | — |
| Карточка: кадр | медиа | да | — | серый плейсхолдер |

**Что если длиннее.** На мобилке это лента карточек по 80% экрана. Разная длина описаний = разная
высота карточек = экран прыгает при листании. Либо лимит, либо фиксированная высота с обрезкой —
выбрано **лимит**, он честнее.

**Крайние значения (19.08).** Компонент шаблонный: «виды» бывают не только у тиснения.

- 0–1 — секция **не выводится**: одна карточка сиротеет в трёхколоночной сетке (376px и пустые две
  трети ряда), а блок без сравнения бессмысленен по смыслу
- 2–3 — сетка: карточки **не растягиваются**, прижаты слева, стрелок нет
- 4 и больше — **карусель**, нецикличная. Стрелки в правом верхнем углу, напротив заголовка; левая
  неактивна на первом кадре, правая на последнем. Верхнего потолка нет — лента тянется

Карточка в карусели остаётся шириной в треть, ровно три в кадре. Ужимать, чтобы четвёртая
выглядывала краем, **не делаем** — что список продолжается, показывают стрелки.

На мобилке ничего не меняется: там лента со свайпом при любом количестве, стрелок нет.

### 2.5. Цены + калькулятор

> **Источник — ХП (CRM/ERP), он же правда.** Подробности и открытые вопросы — в `QUESTIONS.md` §1.
> Ручную таблицу не заводим: менеджер прямо предупредила — «цены меняем, а про табличку никто не
> вспомнит; в сезон нередко делаем несколько повышений».

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Строка прайса: название | **из ХП по API** | — | 24 | блок скрыт |
| Строка прайса: срок | **из ХП по API** | — | 16 | плашка не рендерится |
| Строка прайса: цена мин. заказа | **из ХП по API** | — | — | блок скрыт |
| Строка прайса: минимальный тираж | **из ХП по API** | — | — | откат на «от N ₽/шт» |
| Строка прайса: цена за штуку | **из ХП по API** | — | — | расшифровка не рендерится |
| Оговорка «цены за наши изделия» | текст | да | 120 | — |
| Калькулятор: **URL страницы** | ссылка | нет | — | **пусто → весь блок калькулятора СКРЫТ** |
| Калькулятор: заголовок | текст | да* | 30 | — |
| Калькулятор: подпись | текст | нет | 90 | не рендерится |
| Калькулятор: предустановка | текст (параметр) | нет | — | ссылка ведёт в общий калькулятор без предустановки |

> **Блок калькулятора показывать, ТОЛЬКО когда есть URL страницы калькулятора.** Сейчас страницы
> калькулятора **не существует** — значит поле URL пустое, и весь блок (заголовок + кнопка
> «Онлайн-калькулятор») **не рендерится**. Это обычный условный вывод «пусто → блок скрыт», как
> у остальных блоков — **отдельный дизайн не нужен, разработчик делает флагом**. Появится страница
> → редактор вписывает URL → блок сам включается. (`*` заголовок обязателен, только когда блок виден.)

**Крайние значения (19.08).** Позиций **2–3**. Это пример цен, а не прайс: полный список живёт в
калькуляторе. Три — ровный ряд в карточке шириной 776px; при двух flex растягивает их по половине
сам, править нечего. Четвёртая позиция переносится на второй ряд, и он оказывается **шире**
первого (карточки тянутся по остатку) — раскладка выглядит сломанной. Меньше двух — секция не
выводится. Побочное при двух: левая карточка ниже, тёмный блок калькулятора тянется по её высоте,
и под кнопкой остаётся пустое поле ~150px.

**Срок — плашка, а не строка текста (20.07).** Он и расшифровка цены оба мелкий серый и стояли
по разные стороны от цены, различаясь только местом; плашка разводит их оформлением.
Следствие для редактора: **лимит 16 знаков тут жёстче обычного** — плашка тянется по тексту, и
длинное значение («от 10 рабочих дней») распирает её на всю ширину карточки, превращая в полосу.
Формат значения держать коротким: «2–3 дня», «от 5 дней».

**Строка прайса: три значения, не одно (правка 20.07).** Главная цифра — стоимость минимального
ЗАКАЗА, цена за штуку ушла во вторую строку: «**от 900 ₽** · за 50 шт · от 18 ₽/шт». Причина —
замечание маркетолога: «от 18 ₽/шт» не даёт клиенту понять порог входа, непонятно, сколько всего
платить. Поэтому строке нужны цена мин. заказа + минимальный тираж + цена за штуку.
**Что если ХП отдаёт только цену за единицу** — тогда либо считаем произведение на нашей стороне
(и всё равно нужен минимальный тираж), либо откатываемся на прежний формат «от N ₽/шт». Вопрос
задан разработчику ХП — `QUESTIONS.md` §1 п. 8, помечен как блокирующий.
**В прототипе тираж 50 шт у всех трёх строк — ПЛЕЙСХОЛДЕР** (взят из блока цифр «50 шт минимальный
тираж»), у обложек каталогов и упаковки реальный минимум почти наверняка выше.

**Модель данных (уточнено 17.07).** Мы не продаём технологию и не продаём материал —
**продаётся изделие**, тиснение и бумага это его свойства. Поэтому строка прайса = **изделие +
свойство** («Визитки с тиснением — от 18 ₽/шт»), а калькулятор **один на сайт**: страница передаёт
в него предустановку (`тиснение`, `бумага X`). В ХП тиснение и заведено как **опция калькулятора
продукции**, а не отдельный калькулятор — модель сайта совпадает с моделью ХП.

**Осторожно, бизнес-риск.** Тиснение на изделии клиента — в ХП **другая позиция**, цены заметно
выше. Показываем цены за наши изделия (решение директора) → оговорка обязательна, иначе клиент со своей
упаковкой придёт с чужим ожиданием.

### 2.6. Портфолио

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Работы | **связь** (`work`, флаг «показывать здесь») | нет | — | блок скрыт |
| Работа: тег | текст | нет | 16 | не рендерится |
| Работа: подпись | текст | да | **24** (2 строки) | — |
| Работа: превью | медиа | да | — | работа не рендерится |
| Длительность видео | текст | нет | 5 | бейдж не рендерится |
| Источник видео | список: ВК / YouTube | да (у видео) | — | считается «ВК» |
| ID ролика | текст | да (у видео) | — | **работа не рендерится**: карточка без ролика открывать нечего |

**Ролики не заливаются на сайт.** Они лежат в сообществе ВК (или на YouTube), у работы хранится
только id — тот хвост, что стоит в адресе ролика: `vkvideo.ru/video-29011584_`**`456239782`**,
`youtu.be/`**`dQw4w9WgXcQ`**. Сообщество (`oid`) одно на весь сайт и живёт в настройках, не у работы.
Превью — отдельная картинка, кадр из ролика: сам плеер до клика не грузится, и брать превью
неоткуда.

**Крайние значения.** Вкладки «Видео» и «Фото»: если в одной 0 работ — вкладка не рендерится; если
0 в обеих — блок скрыт. 1–2 работы → лента не прокручивается, это нормально.

**Что видит человек по клику.** Ролик открывается поверх страницы, во весь экран на телефоне;
тег и подпись работы едут в лайтбокс как есть — то есть подпись длиннее 24 знаков обрежется там
на второй строке. Соседние ролики листаются стрелками, порядок — тот же, что в ленте.

### 2.7. Требования к макету (услуга, продукция)

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Заголовок | текст | да | 40 | — |
| Пункт чек-листа: заголовок | текст | да | 30 | — |
| Пункт: пояснение | текст.область | да | 110 | — |
| Файл: название | текст | да | 40 | — |
| Файл: файл | медиа (PDF/AI) | да | — | строка не рендерится |
| Ссылка «полные требования» | ссылка | нет | 50 | не рендерится |

**Крайние значения.** Чек-лист 4 пункта (ориентир), 2–6 держит. Файлов 0 → колонка «Готовые
материалы» скрыта целиком вместе с заголовком.

### 2.7′. Характеристики (материал; альтернатива 2.7)

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Строка «параметр → значение» | повторитель | да | параметр 24, значение 20 | строка не рендерится |
| Примечание | текст.область | нет | 120 | не рендерится |

**Слот взаимоисключающий:** «Требования» отвечают «как прислать файл», «Характеристики» — «что это
за материал». У материала макета нет; у продукции могут понадобиться оба — тогда выводятся подряд.

### 2.8. ЧАВО

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Вопрос | текст | да | **60** | — |
| Ответ | текст.область | да | 400 | — |
| Форма вопроса | глобальная | — | — | — |

**Поведение.** Аккордеоны **эксклюзивные** — открыт всегда один (нативно, атрибутом `name`,
без JS). Первый открыт по умолчанию. Тап по ответу сворачивает.

**Крайние значения.** 3–7 вопросов. 1 → блок выглядит бедно, но работает. 15+ → блок длиннее
разумного, стоит увести часть в отдельную страницу ЧАВО.

### 2.9. Где применяется

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Группы | **выборка по связи** (флаг «применимо») | — | подпись = `short_name` ≤ **20** | блок скрыт |

**Не рукописный список.** Редактор ставит флаг у группы каталога — страница собирает блок сама.
Рукописные плитки разъезжаются при первой правке каталога, и это уже случилось: до 17.07 в блоке
лежал винегрет из разделов, групп и товаров вперемешку (12 плиток, три уровня иерархии).

**Уровень — группа товаров.** Раздел врёт (тиснение не применяется ко всей «Широкоформатной
печати»), товар слишком узок и нестабилен. Группа — тот уровень, на котором клиент решает.

**Крайние значения (19.08).** **1–12** плиток, норма 6.

- 0 — секция не выводится
- 1–5 — как есть, ряд заполнен частично; ничего не растягиваем
- 6 — ровный ряд на десктопе (`repeat(6,1fr)`), три ряда по 2 на мобилке
- 7–11 — 6 + остаток, **дыра в последнем ряду допустима** (решение Omar: показываем как есть)
- 12 — максимум, всё сверх шаблон не выводит

**Мобильная добавка.** 12 плиток на 390px — это 2 колонки × 6 рядов = 1223px, почти полтора экрана
на одном блоке. Поэтому на мобилке выводим **первые 6** (605px), под ними кнопка **«Показать ещё»**
во всю ширину — без числа в подписи. Кнопка китовая, `.mk-btn--ghost`, новую не рисуем; клик
дораскрывает остаток, кнопка исчезает. На десктопе кнопки нет: 12 умещаются в два ряда.

Горизонтальную ленту со свайпом здесь **отвергли**: плитки — ссылки в каталог, лента прячет две
трети из них за жест, а вертикальный список листается тем же движением, что и вся страница.

### 2.10. С чем сочетается

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Технологии/отделки | **связь** | нет | заголовок 30, подпись 20 | блок скрыт |

**Крайние значения (19.08).** **2–6** плиток, одинаково на десктопе и на мобилке.

- 0–1 — секция не выводится: «сочетается» с одной позицией не бывает
- 2–5 — как есть, плитка держит треть ряда. Дыра в неполном ряду не мешает: плитка низкая (117px),
  вертикальной пустоты не остаётся
- 6 — норма и максимум: два ровных ряда по 3, на мобилке 761px

**Растяжку при неполном ряде здесь не делаем** — в отличие от «Статей». Плитка горизонтальная,
кадр слева фиксированные 140px, и при растяжке между подписью и стрелкой вырастает пустое поле:
одна плитка на 1152px выглядит плохо, две по 568px — хуже, чем те же две по 376px.

На мобилке 1 в ряд (иначе не выйдет: кадр 140px плюс текст, две колонки на 390px дали бы по
175px). Каруселей и кнопок «показать ещё» в этом блоке нет.

**Лимиты пересчитаны 07.08 замером,** старые (20/40) были взяты не оттуда: текстовая колонка
плитки — 170px на десктопе и 160px на телефоне. Заголовку отведены две строки — это ~30 знаков,
дальше многоточие; **одно слово должно быть ≤16 знаков**, иначе переносить негде и оно обрежется
краем без многоточия. Подпись всегда одна строка — ~20 знаков («Прочный клеевой корешок», 23
знака, на 390 уже с многоточием). Высота плитки на лимиты больше не реагирует: она фиксирована.

### 2.11. Отзывы

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Отзывы с карт | **внешний виджет SmartWidgets** | — | — | остаются статичные карточки-фолбэк |
| Оценка (сейчас «5,0») | текст | да | 3 | блок оценки не рендерится |
| Ссылка «Добавить отзыв» | ссылка | нет | — | кнопка не рендерится |

**Как устроено.** Виджет рендерит свой DOM в скрытый `.sw-app`, нам нужны только данные:
после события `swAppComplite` они лежат в `window.swapp_data.WidgetReviewsAll`. Мы рисуем **своей**
разметкой (`.mk-review`), чужой свайпер не используем.

**Оценка — поле редактора, и иначе нельзя.** Посчитать её из виджета невозможно, проверено на живых
данных 19.08: `stat` пустой, в сводке по площадкам (`item`) лежат только адрес, контакты и рубрики,
а в `items` приезжают **одни пятёрки** (72 из 72) — среднее по ним всегда даст 5,0. Поэтому цифра
вводится руками и сверяется с картами при ревизии. На 19.08 реальная оценка компании — **5,0**:
Яндекс.Карты 5,0 при 575 оценках, 2ГИС 5 при 31. В прототипе до этого стояло 4,9 — устаревшая
цифра, исправлена. Количество отзывов рядом с ней, наоборот,
считается честно — из `count` по площадкам (сейчас 631), со склонением («631 отзыв», «632 отзыва»).

**Оценка — только по картам.** Свои отзывы с сайта в неё **не подмешиваем**: цифру можно сверить
с Яндексом в один клик (карточки отзывов кликабельные), а свои отзывы модерируем мы — смешанная
оценка перестаёт быть доказательством. Плюс звёздную разметку в выдаче можно ставить только на
свои отзывы, на смешанную нельзя вообще.

Отдельно: **автоматический расчёт по данным виджета не спасёт, даже когда цифра совпадает.**
Сейчас среднее по `items` даёт те же 5,0 — но лишь потому, что виджет отдаёт одни пятёрки. Упадёт
реальный рейтинг до 4,7 — на сайте останется 5,0, и никто этого не заметит.

**Свои отзывы.** На mdmprint.ru у услуги есть собственный поток отзывов (WooCommerce, привязка к
странице, форма с именем, почтой, текстом, файлами и согласием) — сейчас пустой, висит «Ещё нет
отзывов». Заводим его и у нас: кнопка **«Добавить отзыв»** в шапке блока ведёт на эту форму, а не
на карты. Свои отзывы встают в ленту карточками с пометкой «Отзыв на сайте» (вид готов) — то есть
отдельными карточками, но не отдельным рейтингом. Пока своих нет — пустое «Ещё нет отзывов»
**не выводим**, показываем ленту с карт и кнопку.

**Ограничения (важно для ожиданий).** Фильтра по услуге у виджета **нет** — отзывы с карт на всех
страницах будут одни и те же, они про компанию. По услуге можно фильтровать только свои отзывы,
когда они появятся. Форма отзыва у самого виджета есть, но выключена.

**Форма «Добавить отзыв» (19.08).** Модалка на той же механике, что «Заказать звонок» и «Оставить
заявку» (`setupLead`), телефон в ней не спрашиваем — отзыв не заявка.

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Оценка | звёзды 1–5 | да | — | форма не отправляется |
| Имя | текст | да | 40 | форма не отправляется |
| E-mail | почта | нет | — | не рендерится (нужен модератору, на сайте не публикуем) |
| Отзыв | текст.область | да | 1200 | форма не отправляется |
| Фото работы | медиа, до 10 | нет | JPG · PNG · WEBP | блок фото не рендерится |
| Согласие на обработку ПДн | флаг | да | — | форма не отправляется |
| Согласие на новости | флаг | нет | — | — |

Поля повторяют форму отзыва на mdmprint.ru, **кроме оценки звёздами** — там её нет. Добавили
намеренно: карточки отзывов у нас рисуют звёзды, своим отзывам их иначе взять неоткуда, и без
оценки не собрать `AggregateRating`, ради которого свои отзывы и заводятся.

После отправки — экран «Спасибо за отзыв» с обещанием проверки, форма закрывается кнопкой
«Понятно». Текст обещает публикацию «в течение рабочего дня»: если модерация будет медленнее,
формулировку править (см. вопрос ниже).

**Кто модерирует свои отзывы — вопрос заказчику** (`QUESTIONS.md`): без ответа форма превращается
в отстойник. Кнопку «Оставить отзыв» уже убирали 17.07 при работе над блоком; в доке это значилось
как решение заказчика, но подтверждения в его ответах нет — 19.08 кнопка возвращена в новом виде
(ведёт на свою форму, а не на выбор площадки).

**Крайние значения.** Виджет не отдал минимум годных отзывов → остаются статичные, это штатный
фолбэк, а не ошибка.

### 2.12. Статьи

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Статьи | **выборка по рубрике/связи** | — | заголовок 60 | блок скрыт |
| Рубрика | таксономия | да | 16 | тег не рендерится |
| Время чтения | число | нет | — | не рендерится |

**Крайние значения (19.08).** **2–5** карточек.

- 1 — секция не выводится ни на десктопе, ни на мобилке
- 2–4 — число колонок равно числу карточек: ряд заполняется целиком, карточки растягиваются
- 5 — норма и максимум (десктоп — ряд из 5)
- есть ещё статьи — выводим 5 свежих, остальное за ссылкой «Все статьи»
- мобилка — одна колонка, те же 2–5

В прототипе это сделано без JS и без счётчика в шаблоне: `.art-grid:has(> :nth-child(2):last-child)`
и так далее до четырёх — селектор сам видит, сколько карточек пришло. Правила стоят в
`@media (min-width:1024px)`: на мобилке колонка одна при любом количестве.

Слабое место растяжки — ровно 2 карточки: 568px ширины при 118px высоты, заголовок в одну строку,
«N мин чтения» и стрелка разъезжаются по краям. Сравнили с двумя альтернативами (оставить 216px,
как в ряду из пяти; растянуть до трети через 3 колонки) — **выбрана растяжка**, 19.08 по рендеру.

Блок не заполняется руками: тянем 5 свежих статей рубрики. Тогда переполниться он не может.

### 2.13. Финальный CTA

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| Заголовок (`data-cms="cta_title"`) | текст | да | 40 | — |
| Текст (`data-cms="cta_text"`) | текст.область | да | 140 | — |
| Кнопка (`data-cms="cta_button"`) | текст | да | 24 | — |

В вёрстке эти три поля уже помечены атрибутом `data-cms` — точки подстановки видны в HTML.

---

## 3. Формы (глобальные, не поля страницы)

Три формы: **обратный звонок**, **заявка на расчёт**, **вопрос в ЧАВО**. Поля почти совпадают —
`name`, `phone`, `email`, `company`, `contact_way`, `telegram`, `comment`/`message`, `files`,
`promo`, согласия.

> **Решение (17.07, уточнено 19.08):** свести в **один компонент** с параметрами (заголовок,
> подпись, набор полей). Иначе в WordPress появятся три почти одинаковые формы, и правка валидации
> будет делаться трижды. Сам рефактор делает разработчик при переносе — в прототипе поведение уже
> одинаковое, разница только внутри кода. Контракт, по которому сводить, — в 3.4.

| Что | Требование |
|---|---|
| Honeypot | скрытое поле `company` — уже есть, ботов отсекает без капчи |
| Согласие на обработку ПД | обязательное, ссылки на политику — **реальные URL**, сейчас заглушки |
| Куда уходит заявка | **вопрос к заказчику**: почта, ХП, Битрикс? см. `QUESTIONS.md` §2 |
| Анти-зум iOS | поля 16px — константа платформы, не трогать |
| Тач-цель | ≥44px — соблюдено |

**Пять состояний формы, все свёрстаны:**

| Состояние | Что показываем |
|---|---|
| Пусто | кнопка **активна**, ошибки ещё не показаны (почему так — 3.2 и 3.4) |
| Ошибка полей | подсвечиваются **все** незаполненные разом, фокус уводится на первое |
| Отправляем | кнопка занята, подпись «Отправляем…», второй клик не проходит |
| Не отправилось | плашка над кнопкой, данные остаются в полях, в тексте — телефон как запасной канал |
| Успех | экран «Заявка принята» вместо формы, текст подстраивается под выбранный способ связи |

> **Почему ошибка — плашка, а не экран.** Экран-заглушка увёл бы человека от формы вместе со
> всем, что он ввёл. Повторить отправку надо в одно нажатие, не набирая заново.
>
> **Почему в тексте ошибки есть телефон.** Если лежит сервер, вторая попытка тоже не пройдёт, и
> звонок — единственное, что человеку остаётся. Номер в этом тексте обязан совпадать с номером в
> шапке: это одна константа, а не две строки в разных местах.

**Тексты состояний — не поля редактора,** это часть интерфейса. Редактор меняет заголовок, подпись
и текст кнопки формы (см. 3.1), но не формулировки ошибок: они выверены и в CMS не выносятся.

### 3.1. Контекст обращения — ЕСТЬ ПОЛЕ ДЛЯ РЕДАКТОРА (20.07)

Модалка заявки сообщает, по какой услуге пришёл человек: заголовок читается
«ОСТАВИТЬ ЗАЯВКУ **НА ТИСНЕНИЕ ФОЛЬГОЙ**» (вторая часть тем же капсом, но серым).
Раньше это была отдельная серая плашка — убрана: читалась как метка от системы.

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| `service_accusative` — название услуги в ВИНИТЕЛЬНОМ падеже | текст | да | 40 | заголовок «Оставить заявку» без услуги |
| `context` — название в именительном, скрытым полем менеджеру | из записи | да | — | заявка уйдёт без темы |

> **ГЛАВНОЕ, ЧТО ЛЕГКО ПРОПУСТИТЬ.** «Заявка на …» требует **винительного падежа**, поэтому это
> ОТДЕЛЬНОЕ поле, а не заголовок страницы. У «тиснения фольгой» падеж совпал с именительным
> по роду, и это обманчиво: «ламинация» превратится в «Заявка на **ламинация**»,
> «широкоформатная печать» — в «Заявка на **широкоформатная печать**».
> Правильно: «на ламинацию», «на широкоформатную печать».

**Лимит 40 знаков не косметический.** Заголовок липкий: длинное название растит шапку и съедает
и без того малую высоту при открытой клавиатуре. Замер на 390px: «УФ-лак» — 1 строка,
«тиснение фольгой» — 2, «широкоформатную печать баннеров и рекламных конструкций» — 4.
Переполнения нет ни при каком, но 4 строки — это уже много.

### 3.2. Обратная связь по полям — поведение, а не поле

Кнопка отправки **не блокируется**. Раньше она гасла молча, а сообщение об ошибке жило в
обработчике отправки, который у заблокированной кнопки сработать не может: узнать причину
было нельзя в принципе. Теперь ошибки показываются:

- при **уходе** из поля — только если в нём что-то введено и введено неверно;
- про **незаполненное** — по нажатию кнопки: подсвечиваются все проблемные поля разом,
  и страница прокручивается к первому (внутри шторки свой скролл, одного `focus()` мало);
- гаснет ошибка **сразу**, как человек исправил.

Пустое поле по уходу молчит намеренно: модалка сама ставит фокус в телефон, и первый же тап
в соседнее поле красил его, хотя туда не печатали.

**Имя:** требуется хотя бы одна буква и два знака. Цифры **не запрещены** — у нас B2B, и
«Отдел 5» или «21 век» законные ответы на «как к вам обращаться». Полный запрет цифр —
классическая переусердствованная валидация.

Тексты ошибок — константы интерфейса, в админку не выносить.

### 3.3. Файлы

Лимит **10 файлов**, форматы PDF · AI · CDR · PSD · JPG · ZIP.

Родной `input[type=file]` при каждом выборе **ЗАМЕНЯЕТ** список, а не дополняет: выбрал второй
файл — первый исчез, и убрать лишний нечем. Поэтому список ведётся своим массивом, а поле
пересобирается через `DataTransfer`. При переносе это **нельзя упростить обратно** к родному
поведению — вернётся баг. Дубли отсекаются по имени и размеру, БЕЗ `lastModified`: при повторном
выборе того же файла браузер отдаёт новую метку времени.

---

### 3.4. Единый механизм форм — контракт (19.08)

Вопрос разработчиков: на текущем сайте mdmprint.ru кнопка «Отправить» залочена, у нас нет; свести
бы формы к одному виду и механизму. Сводим — но по НАШЕЙ логике, залоченную кнопку не копируем
(почему — 3.2).

**Что где в прототипе (рефактор сделан 19.08):**

| Место | Механизм | Кнопка |
|---|---|---|
| Модалка «Заказать звонок» | `setupLead()` на общем наборе `fk` | активна всегда |
| Модалка «Оставить заявку» | тот же `setupLead()` | активна всегда |
| Форма ЧАВО | свой блок, правила из того же `fk` | активна всегда |
| Вход по SMS | своя логика, телефон из `fk` | «Войти» залочена, пока не введены 4 цифры |
| Подписка в футере | нативная валидация браузера | активна |

Общее собрано в `fk` (в `usluga.html`, перед `setupLead`): формат и проверка телефона, проверка
почты и имени, подсветка поля, мягкая проверка по уходу, увод фокуса на первое проблемное поле,
отправка с занятой кнопкой и плашкой ошибки. Формам остаётся только своё: у заявки файлы, у ЧАВО
выбор способа связи и шторка на мобилке. Подписка в футере — одно поле почты на нативной
валидации, её сознательно не трогали.

При переносе в WordPress эти же куски становятся общим модулем, а формы — его вызовами.

**Контракт компонента:**

1. **Разметка одна на все формы:** `form` → `.cbm-field` → поле + `.cbm-err`; чекбокс согласия
   помечен `data-role="consent"`; кнопка `type="submit"`.
2. **Один инициализатор** на все формы страницы. Правила валидации привязаны к **типу поля**
   (телефон, почта, согласие, текст), а не к конкретной форме — иначе третья форма опять получит
   свою копию правил.
3. **Кнопка активна всегда.** Атрибут `disabled` в HTML остаётся как страховка на случай, если JS
   не загрузился, и снимается при инициализации. Гасим кнопку только на время отправки.
4. **Ошибки** — по правилам 3.2: по уходу из поля (если в нём печатали), по нажатию — все разом
   с фокусом на первую, гаснут сразу после исправления.
5. **Успех** — в модалке экран «Заявка принята» вместо формы, в инлайн-форме сообщение под ней.

**Где `disabled` остаётся и это правильно:** кнопка «Войти» при вводе SMS-кода (проверять нечего —
либо четыре цифры набраны, либо нет, сообщать об ошибке не о чем) и состояние «Отправляем…»
(защита от двойной отправки).

### 3.5. Куки-баннер (19.08)

Не поле редактора — часть интерфейса, но с юридическими требованиями.

**Почему две кнопки, а не одна.** По 152-ФЗ и разъяснениям РКН cookie с идентификаторами —
персональные данные, значит нужно и информировать, и получить согласие **до** их установки.
Формула «продолжая пользоваться сайтом, вы соглашаетесь» согласием не считается: оно должно быть
конкретным и активным действием. Поэтому «Принять все» и «Только необходимые», причём отказ —
полноценная кнопка, а не серая ссылка мелким шрифтом.

**Что хранит прототип:** выбор, дату и версию текста (`localStorage`, ключ `mdm.cookie.consent`).
Версия нужна, чтобы при переписывании политики поднять её и спросить согласие заново — старое
относилось к другому тексту. **В проде:** cookie со сроком год плюс запись факта согласия на
стороне сервера, доказывать его придётся нам.

**Главное для разработчика:** счётчики аналитики и рекламные пиксели подключаются ТОЛЬКО после
«Принять все». При «Только необходимые» — ничего, кроме технических cookie. Сейчас на прототипе
счётчика нет, поэтому в коде только запись решения — место, куда встраивать, помечено
комментарием.

**Поведение в интерфейсе.** Полоса внизу, `z-index` 95 — выше липкой полосы заявки (90), ниже
модалок (100). Пока выбор не сделан, липкая полоса не показывается вовсе: это один и тот же угол
экрана, и два слоя друг на друге заставляют выбирать между «принять» и «оставить заявку».
Текст ссылается на политику обработки данных — **ссылка сейчас заглушка**, нужен реальный URL.

## 4. Что НЕ поле, а константа дизайн-системы

Редактор этим **не управляет**, и в админку выносить не надо: цвета, отступы, радиусы, ступени
шрифта, форма кнопок и иконок. Всё в `assets/css/tokens.css` + `kit.css`, правила — в
`DESIGN_SYSTEM.md`. Если в задаче появится «сделать заголовок покрупнее вот здесь» — это правка
ступени в токенах, а не поле в админке.

---

## 5. Чек-лист приёмки блока

- [ ] Каждое поле имеет лимит, счётчик и понятную подпись в админке.
- [ ] Пустое необязательное поле не оставляет дырок в вёрстке.
- [ ] Пустой блок скрывается целиком **вместе с пунктом в подшапке**.
- [ ] Крайние значения (0 / 1 / максимум) проверены рендером, а не рассуждением.
- [ ] Данные из связей, а не рукописные списки — там, где так указано.
- [ ] Тексты не подставляются в HTML без экранирования.

---

## 6. SEO — поля редактора и микроразметка

Аудит прототипа 23.07 нашёл пустоту: у страницы не было ни `description`, ни `canonical`, ни своей
микроразметки. Единственный `JSON-LD` в DOM приходил от **стороннего виджета отзывов** и описывал
не нашу страницу. В эталон `prototypes/usluga.html` добавлен рабочий образец — ниже состав.

### 6.1. Поля редактора

| Поле | Тип | Об. | Лимит | Пусто |
|---|---|---|---|---|
| `seo_title` | текст | нет | **60** | берётся `H1` + « — MDMprint» |
| `seo_description` | текст.область | нет | **160** | **не выводить пустой тег** — поисковик соберёт сниппет сам; это лучше, чем пустой `content=""` |
| `og_image` | медиа | нет | 1200×630 | подставить первое фото портфолио, иначе — логотип на фирменном фоне |

**Почему лимиты именно такие.** 60 и 160 — не наша выдумка, а ширина сниппета в выдаче: длиннее
обрезается многоточием, и обрывается ровно на середине мысли. Счётчик в админке обязателен,
как и у остальных полей.

**`canonical` редактор не заполняет** — генерится из постоянной ссылки записи. Ручное поле здесь
опаснее пользы: одна опечатка склеит страницу с чужой и уберёт её из выдачи.

### 6.2. Микроразметка — генерится, а не пишется

Две сущности, обе **уже есть на странице видимым текстом**:

- `BreadcrumbList` — из тех же данных, что рисуют крошки. Даёт в выдаче путь вместо голого URL.
- `FAQPage` — из блока «Частые вопросы». Даёт раскрывающиеся вопросы прямо в сниппете.

> **Правило:** разметка собирается из **тех же полей**, что и видимый блок. Отдельного поля
> «текст для микроразметки» быть не должно: расхождение разметки с видимым текстом — прямое
> нарушение правил и Яндекса, и Google, вплоть до снятия расширенного сниппета.
> Скрыли блок ЧАВО в админке — `FAQPage` тоже не выводится.

`Product` от виджета отзывов не трогаем: чужой скрипт, чужая разметка. Но проверить, что он не
конфликтует с нашим `@graph`, — задача разработчика на этапе интеграции.

### 6.3. Что осталось за пределами прототипа

| Пункт | Чья зона | Комментарий |
|---|---|---|
| `robots.txt`, `sitemap.xml` | разработка | к дизайну отношения не имеют, но в тикетах не описаны |
| Реальные URL вместо `href="#"` | CMS | в прототипе около 600 ссылок-заглушек — это норма для прототипа |
| ЧПУ и структура адресов | заказчик + разработка | влияет на `canonical` и крошки |
| Картинки виджета отзывов без `width`/`height` | вендор | ~20 шт, разметку выдаёт `smartwidgets.ru`, править нечего |
| Заголовки колонок футера — `H5` после `H3` | принято как есть | на ранжирование не влияет (порядок заголовков не фактор), для скринридера мелочь. Чинится только сменой тега, а к нему привязан `nth-of-type` в раскладке колонок — правка дороже пользы |

**Что уже сделано в эталоне (не нужно делать заново):** `description`, `canonical`, четыре `og:*`,
`JSON-LD` с крошками и ЧАВО, один `H1`, `alt` у всех своих картинок, `width`/`height` у логотипов и
аватаров, ленивая загрузка, `rel="noopener"` у внешних ссылок, разделы каталога в мегаменю —
настоящие `<a href>` (были `<div>`, то есть верхний уровень каталога выпадал из индекса).
